-- function lightning_shift(event)
--     if event.entity.prototype.name == "zeus-wrath-reverse-lightning" then
--         -- game.print("Hew I spawned")
--         -- event.entity.position["y"] = event.entity.position["y"] - 25
--         local new_position = {
--             event.entity.position["x"], event.entity.position["y"] - 5
--         }

--         event.entity.teleport(new_position)

--     end
-- end

-- script.on_event(defines.events.on_trigger_created_entity, lightning_shift)
